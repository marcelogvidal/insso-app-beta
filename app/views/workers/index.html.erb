    <%= render partial: "elements/sidebar" %>
<div class="container-fluid">

  <div class="panel panel-default">
    <div class="panel-body">
      <div class="row">
        <div class="col-lg-12">
          <h3 class="page-header" >Trabajadores
            <small>registrados por empresa y división.</small>
          </h3>
          <table>
            <thead>
              <tr>
                <th>Name</th>
                <th>Lastname</th>
                <th>Company</th>
                <th>Division</th>
                <th>Rut</th>
                <th>Bpstest</th>
                <th>File</th>
                <th colspan="3"></th>
              </tr>
            </thead>

            <tbody>
              <% @workers.each do |worker| %>
              <tr>
                <td><%= worker.name %></td>
                <td><%= worker.lastname %></td>
                <td><%= worker.company %></td>
                <td><%= worker.division %></td>
                <td><%= worker.rut %></td>
                <td><%= worker.bpstest %></td>
                <td><%= worker.file %></td>
                <td><%= link_to 'Show', worker %></td>
                <td><%= link_to 'Edit', edit_worker_path(worker) %></td>
                <td><%= link_to 'Destroy', worker, method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
              <% end %>
            </tbody>
          </table>

          <br>
        </div>
      </div>
    </div>

    <%= link_to 'New Worker', new_worker_path %>
