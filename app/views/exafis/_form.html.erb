<%= simple_form_for(@exafi) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :peso %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
